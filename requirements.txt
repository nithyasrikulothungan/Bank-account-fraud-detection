# IMPORTANT: replace these with the EXACT versions from your training environment
# by running `pip freeze | grep -iE "scikit-learn|catboost|pandas|numpy|joblib"`
# in the notebook's environment, and pasting the results here.
# Mismatched scikit-learn versions in particular can cause scaler.pkl to fail
# to unpickle correctly, or unpickle "successfully" while behaving differently.
#
# The versions below are reasonable current defaults — treat them as a
# starting point to verify against your actual training environment, not a guarantee.

flask==3.0.3
flask-cors==4.0.1
catboost==1.2.5
scikit-learn==1.4.2
pandas==2.2.2
numpy==1.26.4
joblib==1.4.2
gunicorn==22.0.0
